import 'package:flutter/material.dart';

class AskedQ extends StatelessWidget {
  const AskedQ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        // ignore: prefer_const_constructors
        appBar: AppBar(title: Text('asked questions')));
  }
}
